pipeline {
    agent { docker { image 'golang' } }
    stages {
        stage('build') {
            steps {
                sh 'go build cmd/reverseproxy/*.go'
                sh 'go build cmd/server/*.go'
            }
        }
    }
}
